#!/bin/bash

# Config section

PREFIX='synchweb'
HOST_WORK_DIR=$(dirname $(dirname $(realpath $0)) | sed "s/docker/SynchWeb/g")
CONT_WORK_DIR='/var/www/sites/synchweb'
HOST_FS_DIR='/media/dls'
CONT_FS_DIR='/dls'
SSH_TUNNEL='user@example.com'

# 'Do not fiddle with' section

THIS_SCRIPT=$(basename $0)
CONT_NAME="${PREFIX}-$1"
RUN_NAME="run-${CONT_NAME}"
BACKEND_RUN_NAME="run-${PREFIX}-php"
